# EsproKurt codebase

A arquitetura do EsproKurt é monolítica e este é o monorepo principal.

## Instruções

Para rodar o frontend:

```bash
pnpm install
pnpm dev:front
```

Para rodar o backend:

```bash
pnpm install
pnpm dev:back
```
